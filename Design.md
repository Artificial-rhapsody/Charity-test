## 一、风格指纹（一句话定位）

非营利与公益营销风 + 经典大色块拼接 + 现代叙事排版。采用优雅衬线标题与沉稳冷色调，传递出值得信赖、充满人文关怀与通透克制的品牌调性。

---

## 二、色彩系统

### 背景层次

| 层级 | 用途 | HEX |
| --- | --- | --- |
| 页面底色 | 基础白（Initiatives区块） | `#FFFFFF` |
| 次级底色 | 浅灰（内容通栏背景） | `#EFEFEF` |
| 深色区块 | 左侧侧边栏/全宽色块（如Donate） | `#2C3E6B` |
| 调和色块 | 视觉过渡色块（如Impact） | `#9BBFC2` |

### 语义色彩

* **主色（Primary）**：`#2C3E6B` — 用于深色区块底色、卡片头部标题背景。
* **辅助色**：`#9BBFC2` — 用于特定的过渡色块底色。
* **强调色**：`#A855F7`（浅紫） — 用于首屏标题边框、进度条及核心募捐按钮。

### 文字色阶

* 主要正文：`#1A1A1A`（接近纯黑，用于亮底文字）
* 次要说明：`#555555`（深灰，用于卡片内文及次要信息）
* 反白文字：`#FFFFFF`（用于深色色块及页脚上的文字）

### 边框与分割

* 边框色：`#A855F7`（首屏标题框，粗细约 3px，实线）；常规卡片无显式边框，依靠色块区分。

### 色彩规律总结

* 整体色温：中性偏冷（深蓝与青绿主导，局部点缀暖色图片调和）。
* 对比策略：高对比（深蓝/白、深蓝/浅灰交替，增强区块可读性）。
* **可复用的配色公式**：`深海军蓝 (#2C3E6B) + 灰青绿 (#9BBFC2) + 基础浅灰 (#EFEFEF) + 局部高饱和紫/粉点缀`。

---

## 三、字体与排版系统

### 字体选型

* 标题字体：衬线体（Serif），推测：*Georgia* 或 *Playfair Display*
* 正文字体：无衬线体（Sans-serif），推测：*Arial* 或 *Helvetica*
* 是否混排多种字体：是（标题衬线增强故事感，正文无衬线保证易读性）

### 字号体系

```
--text-hero:     56px  /* 首屏大标题 "A lifelong commitment..." */
--text-h1:       42px  /* 模块大标题 "Success stories" 等 */
--text-h2:       24px  /* 卡片/子项标题 "Education" 等 */
--text-body:     16px  /* 标准正文 */
--text-small:    14px  /* 署名/次要标签 */

```

### 字重规律

* 标题惯用字重：`600` / `700`（粗体衬线）
* 正文惯用字重：`400`（常规）
* 是否使用超细字重：否，采用标准字重确保无障碍阅读。

### 排版节奏

* 标题行高：约 `1.1` ~ `1.2` 倍
* 正文行高：约 `1.5` ~ `1.6` 倍
* 字间距：标题标准 / 正文标准
* 是否使用全大写（ALL CAPS）：否。
* 文字对齐主风格：混合。左侧标题栏文字左对齐，右侧内容区根据组件性质（如Initiatives卡片居中，故事列表左对齐）。

---

## 四、布局与间距系统

### 整体结构

* 最大内容宽度：约 `1200px`
* 水平留白（两侧 padding）：约 `60px`
* 栅格：常态下为不规则左右双栏（40% : 60%）或等宽三列（卡片区）。

### 间距节奏

```
--space-sm:   16px
--space-md:   24px
--space-lg:   40px
--space-xl:   80px
--space-2xl:  120px

```

* 区块间距（section gap）：约 `0px`（采用无缝色块拼接）
* 标题到正文：约 `24px`
* 卡片内边距：约 `24px`（Initiatives卡片底部内衬）

### 布局风格特征

* 主要布局模式：左右非对称分栏 + 通栏大色块交替拼接。
* **可复用规律**：核心模块采用“左侧 1/3 纯色块承载标题，右侧 2/3 浅色块承载多行内容列表”的左右通栏对开布局。

---

## 五、核心组件样式规范

### 按钮

| 类型 | 背景 | 文字色 | 圆角 | 内边距 | 字重 | 特效 |
| --- | --- | --- | --- | --- | --- | --- |
| Card CTA | `#9BBFC2`（半透明感） | `#FFFFFF` | 0px | 8px 16px | 500 | 无 |
| Widget Donate | `#A855F7` | `#FFFFFF` | 4px | 12px 24px | 600 | 无 |

### 卡片

* 背景：`#2C3E6B`（下方底色）/ 上方为视频封面图，圆角：`0px`
* 边框：无
* 阴影：`none`
* 内边距：`24px`（文案区域）

### 募捐进度条微件（Donate Widget）

* 背景：`#FFFFFF`，圆角：`12px`，带有极淡的描边或阴影。
* 内部进度条：灰色底槽 + `#A855F7` 实色填充条。

---

## 六、视觉装饰语言

### 背景装饰

* 纯色块拼接：模块之间无传统空隙，通过高对比色块直接碰撞。
* 真实自然意象：首屏与页脚采用植物、泥土、手部特写等高精细度大图，打破纯色块的生硬感。

### 阴影与深度

* 阴影风格：整体扁平化（Flat），仅募捐微件组件带有轻微浮雕感，其余卡片、色块均无阴影。

### 模糊与透明

* 视频播放按钮：白色半透明蒙层（`rgba(255,255,255,0.3)`）圆圈包裹白色播放三角。

### 图形语言

* 图标风格：极简社媒图标（如页脚 Facebook, Instagram 线性图标）。
* 插画/图形：首屏标题处使用了大框线包裹装饰。

---

## 七、交互与动效线索

* **卡片/视频 hover 迹象**：预估视频封面图在 hover 时播放按钮会有放大或不透明度加深效果；CTA 按钮颜色会有深浅微调。
* **过渡风格推断**：配合沉稳调性，推荐使用 `ease-in-out`，时间约为 `250ms` 的平滑渐变过渡。

---

## 八、模块级视觉规律（从上到下拆解）

### [模块 1] Hero 首屏

* 采用不规则对开。右侧为倾斜剪切的大型人物斜切图；左侧为纯白背景，核心标题使用高饱和紫色的正方形粗框线进行视觉聚焦，下方承载两行说明。

### [模块 2] Initiatives 倡议区

* 纯白通栏背景，顶部居中衬线标题。下方呈现三列卡片，卡片无圆角，上半部分为网格视频/图片，下半部分为深蓝底色反白字，底部配有浅色扁平矩形按钮。

### [模块 3] Success stories 成功故事

* 经典左右非对称分栏。左侧 1/3 为深蓝底色，反白大标题；右侧 2/3 为浅灰背景，垂直排列多行故事，每行采用“左图右文（包含引言体叙事与署名）”的结构。

### [模块 4] Impact metrics & reports 数据报告

* 结构同上，但左侧 1/3 切换为灰青绿底色（`#9BBFC2`）；右侧列表文字加粗展示报告类别，配有带右箭头的文本链接。

### [模块 5] Donate 募捐区

* 左右非对称分栏。左侧 1/3 延续深蓝底色，右侧 2/3 为浅灰背景，中心放置一个高拟真度的白色募捐卡片微件，形成视觉绿洲。

### [模块 N] Contact us 联系我们

* 全宽通栏大图背景（暗色泥土与植物），文字整体反白（白色），通过高对比度的自然背景为整个网页收尾。

---

## 九、可复用设计规律提炼（核心输出）

1. **大色块拼贴无缝布局**：模块与模块之间不留白，利用高对比色块（深蓝、灰绿、浅灰、纯白）直接相撞区分视区，视觉冲击力强且现代感足。
2. **非对称两栏逻辑**：后续多个模块统一遵循“左侧 1/3 纯色块定调/放标题，右侧 2/3 浅色底放具体内容”的框架，建立极强的浏览秩序感。
3. **衬线与无衬线混排**：大标题、模块标题统一用优雅的 Serif 字体强化故事性与人文调性；正文与标签采用 Sans-serif 确保高阅读效率。
4. **刚性无圆角卡片**：除特定交互微件外，首屏斜切、视频卡片、色块边缘均采用 0 弧度的直角，给人以严谨、克制的结构感。
5. **局部高亮边框聚焦**：在纯白或单色背景上，使用细线彩色方框包裹核心标语（如首屏），能在不借助复杂动效的前提下快速锁定视线。

---

## 十、CSS 变量速查表

```css
:root {
  /* 色彩 */
  --color-bg-pure:      #ffffff;
  --color-bg-light:     #efefef;
  --color-bg-dark:      #2c3e6b;
  --color-bg-teal:      #9bbfc2;
  --color-accent:       #a855f7;
  --color-text-main:    #1a1a1a;
  --color-text-muted:   #555555;
  --color-text-white:   #ffffff;

  /* 排版 */
  --font-display:       'Georgia', serif;
  --font-body:          'Arial', sans-serif;
  --text-hero:          56px;
  --text-h1:            42px;
  --text-h2:            24px;
  --text-body:          16px;
  --text-small:         14px;

  /* 形态 */
  --radius-none:        0px;
  --radius-widget:      12px;
  --border-thickness:   3px;

  /* 最大宽度 */
  --max-width:          1200px;
}

```

---

## 十一、仿作注意事项

* **最难还原的 2～3 个视觉细节**：
1. 首屏大图的斜切角度与左侧文案在响应式布局下的空间重叠平衡，需精细调试 `clip-path` 或绝对定位。
2. 连续多个模块进行左右分栏时，左侧色块与右侧内容区的比例在不同屏幕尺寸下需严格对齐，否则极易松散。


* **推荐技术方案**：
* CSS 框架：强烈建议使用原生 CSS Custom Properties 配合 Flexbox / CSS Grid 轻易实现无缝色块拼接与对开分栏。
* 字体引入：可采用 Google Fonts 引入 *Playfair Display*（作为 `--font-display`）与 *Inter* 或系统自带 *Arial*（作为 `--font-body`）。